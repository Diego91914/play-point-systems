# Inside Man launch content

The initial scenario pool contains **60 unique base scenarios**. A five-mission match samples five without replacement. Each mission also assigns player-specific private targets, so repeated base scenarios can create materially different table discussions in later matches.
