# Vercel production redeploy marker

Created after Vercel plan upgrade to trigger a fresh production deployment from `main` so the merged builder-session middleware exemption is included in production.
