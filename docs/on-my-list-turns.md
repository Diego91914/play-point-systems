# Turn flow

The app randomizes the non-surveyed players at the start of every board. Each active guesser gets one spoken guess per turn. A miss advances immediately; a second miss removes that player from the board. A GOT IT pauses turn advancement until the Surveyed Player selects the matching hidden answer, then play advances to the next active guesser.
